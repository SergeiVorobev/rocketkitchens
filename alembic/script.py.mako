"""${alembic_utils.get_docstring_header()}
${alembic_utils.get_docstring_revision(revision, is_upgrade=True)}
${alembic_utils.get_docstring_down_revision(down_revision)}

import sqlalchemy as sa
from alembic import op

# This is a placeholder migration script.
# Replace this code with your own migration
# code to make changes to your database schema.

def upgrade():
    pass

def downgrade():
    pass
